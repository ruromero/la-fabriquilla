network:
  default: deny
  allow:
    - endpoint: "localhost:11434"
      methods: [POST]
      paths: ["/v1/chat/completions"]
    - endpoint: "api.github.com:443"
      methods: [GET, POST, PATCH]
      paths: ["/repos/"]
