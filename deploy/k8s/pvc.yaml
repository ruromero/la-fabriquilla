apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fabriquilla-state
  namespace: fabriquilla
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
